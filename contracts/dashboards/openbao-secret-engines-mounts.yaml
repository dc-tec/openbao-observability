version: v0.1
maturity:
  lifecycle: draft
  evidence:
    - generated-validated
uid: openbao-secret-engines-mounts
title: OpenBao secret engines and mounts
refresh: 30s
tags:
  - audit
  - generated
  - mounts
  - openbao
  - reference
  - secret-engines
timeRange:
  from: now-1h
  to: now
datasources:
  metrics:
    type: prometheus
    uid: prometheus
  logs:
    type: loki
    uid: loki
variables:
  - name: request_id
    label: Request ID
    type: textbox
    default: .*
  - name: engine_path
    label: Engine or mount path
    type: textbox
    default: .*
  - name: operation
    label: Operation
    type: custom
    default: .*
    options:
      - .*
      - read
      - list
      - create
      - update
      - delete
  - name: node_id
    label: Node
    type: textbox
    default: .*
panels:
  - id: secret-engine-events
    title: Secret engine events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret|kv-v1|database|transit|pki)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited secret engine events matching the selected filters over 5 minutes.
    grid:
      x: 0
      y: 0
      w: 6
      h: 4
  - id: mount-lifecycle-events
    title: Mount lifecycle events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"sys/mounts(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited mount table reads and mount lifecycle changes over 5 minutes.
    grid:
      x: 6
      y: 0
      w: 6
      h: 4
  - id: kv-events
    title: KV v1 and v2 events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret/(data|metadata)/.*|kv-v1/.*)" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited KV v1 and KV v2 data and metadata events over 5 minutes.
    grid:
      x: 12
      y: 0
      w: 6
      h: 4
  - id: dynamic-database-events
    title: Database events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"database/(config|roles|creds|rotate-root).*" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited database secrets engine configuration, role, and credential events over 5 minutes.
    grid:
      x: 18
      y: 0
      w: 6
      h: 4
  - id: transit-events
    title: Transit events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"transit/(keys|encrypt|decrypt|rewrap|sign|verify|hmac).*" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited Transit key and cryptographic operation events over 5 minutes.
    grid:
      x: 0
      y: 4
      w: 6
      h: 4
  - id: pki-events
    title: PKI events
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"pki/(roles|issue|issuer|root|cert|tidy|revoke).*" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited PKI engine events when the deployment enables PKI.
    grid:
      x: 6
      y: 4
      w: 6
      h: 4
  - id: secret-engine-errors
    title: Response errors
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json audit_type="type", audit_error="error", operation="request.operation", request_id="request.id", request_path="request.path" | audit_type="response" | audit_error!="" | request_path=~"(secret|kv-v1|database|transit|pki)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited secret engine response entries with error fields over 5 minutes.
    grid:
      x: 12
      y: 4
      w: 6
      h: 4
  - id: denied-secret-engine-requests
    title: Denied requests
    type: stat
    signal: logs
    datasource: logs
    expression: 'sum(count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json audit_error="error", operation="request.operation", request_id="request.id", request_path="request.path" | audit_error=~"(?s).*permission denied.*" | request_path=~"(secret|kv-v1|database|transit|pki)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    unit: short
    description: Shows audited secret engine requests denied by policy over 5 minutes.
    grid:
      x: 18
      y: 4
      w: 6
      h: 4
  - id: mount-table-entries
    title: Mount entries
    type: stat
    signal: metrics
    datasource: metrics
    expression: sum(openbao:core_mount_table_num_entries:max)
    unit: short
    description: Shows total mount table entries from the low-cardinality mount table metric.
    grid:
      x: 0
      y: 8
      w: 6
      h: 4
  - id: mount-table-size
    title: Mount table size
    type: stat
    signal: metrics
    datasource: metrics
    expression: sum(openbao:core_mount_table_size:max)
    unit: bytes
    description: Shows total mount table size from the low-cardinality mount table metric.
    grid:
      x: 6
      y: 8
      w: 6
      h: 4
  - id: kv-secret-count
    title: KV secrets
    type: stat
    signal: metrics
    datasource: metrics
    expression: openbao:secret_kv_count:max30m
    unit: short
    description: Shows the maximum KV secret count observed over 30 minutes when OpenBao exposes the slow usage gauge.
    grid:
      x: 12
      y: 8
      w: 6
      h: 4
  - id: dynamic-lease-creations
    title: Dynamic leases
    type: stat
    signal: metrics
    datasource: metrics
    expression: openbao:secret_lease_creation:increase15m
    unit: short
    description: Shows dynamic secret lease creation events over 15 minutes when OpenBao exposes the secret lease creation counter.
    grid:
      x: 18
      y: 8
      w: 6
      h: 4
  - id: kv-secret-count-by-namespace
    title: KV secrets by namespace
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: openbao:secret_kv_count_by_namespace:max30m
    legend: "{{namespace}}"
    unit: short
    description: Shows KV secret count by namespace over a 30-minute window without grouping by mount path.
    grid:
      x: 0
      y: 12
      w: 12
      h: 8
  - id: kv-route-operation-rates
    title: KV v1 operation rates
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:route_create_kv_v1:rate5m, "kv_operation", "create", "source_prefix", ".*") or label_replace(openbao:route_read_kv_v1:rate5m, "kv_operation", "read", "source_prefix", ".*") or label_replace(openbao:route_list_kv_v1:rate5m, "kv_operation", "list", "source_prefix", ".*") or label_replace(openbao:route_delete_kv_v1:rate5m, "kv_operation", "delete", "source_prefix", ".*")
    legend: "{{kv_operation}}"
    unit: ops
    description: Shows route-derived KV v1 create, read, list, and delete rates for the reference kv-v1 mount.
    grid:
      x: 12
      y: 12
      w: 12
      h: 8
  - id: kv-route-operation-latency
    title: KV v1 operation latency
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:route_create_kv_v1:avg5m, "kv_operation", "create", "source_prefix", ".*") or label_replace(openbao:route_read_kv_v1:avg5m, "kv_operation", "read", "source_prefix", ".*") or label_replace(openbao:route_list_kv_v1:avg5m, "kv_operation", "list", "source_prefix", ".*") or label_replace(openbao:route_delete_kv_v1:avg5m, "kv_operation", "delete", "source_prefix", ".*")
    legend: "{{kv_operation}}"
    unit: ms
    description: Shows average route-derived KV v1 create, read, list, and delete latency for the reference kv-v1 mount.
    grid:
      x: 0
      y: 20
      w: 12
      h: 8
  - id: kv-v2-route-operation-rates
    title: KV v2 operation rates
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:route_create_kv_v2:rate5m, "kv_operation", "create", "source_prefix", ".*") or label_replace(openbao:route_read_kv_v2:rate5m, "kv_operation", "read", "source_prefix", ".*") or label_replace(openbao:route_list_kv_v2:rate5m, "kv_operation", "list", "source_prefix", ".*") or label_replace(openbao:route_delete_kv_v2:rate5m, "kv_operation", "delete", "source_prefix", ".*")
    legend: "{{kv_operation}}"
    unit: ops
    description: Shows route-derived KV v2 create, read, list, and delete rates for the default secret mount.
    grid:
      x: 12
      y: 20
      w: 12
      h: 8
  - id: kv-v2-route-operation-latency
    title: KV v2 operation latency
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:route_create_kv_v2:avg5m, "kv_operation", "create", "source_prefix", ".*") or label_replace(openbao:route_read_kv_v2:avg5m, "kv_operation", "read", "source_prefix", ".*") or label_replace(openbao:route_list_kv_v2:avg5m, "kv_operation", "list", "source_prefix", ".*") or label_replace(openbao:route_delete_kv_v2:avg5m, "kv_operation", "delete", "source_prefix", ".*")
    legend: "{{kv_operation}}"
    unit: ms
    description: Shows average route-derived KV v2 create, read, list, and delete latency for the default secret mount.
    grid:
      x: 0
      y: 28
      w: 12
      h: 8
  - id: pki-issue-rate
    title: PKI issue rate
    type: stat
    signal: metrics
    datasource: metrics
    expression: openbao:pki_issue:rate5m
    unit: ops
    description: Shows certificate issue operations per second over 5 minutes for PKI mounts that expose the normalized pki issue rule.
    grid:
      x: 0
      y: 36
      w: 6
      h: 4
  - id: pki-revoke-rate
    title: PKI revoke rate
    type: stat
    signal: metrics
    datasource: metrics
    expression: openbao:pki_revoke:rate5m
    unit: ops
    description: Shows certificate revoke operations per second over 5 minutes for PKI mounts that expose the normalized pki revoke rule.
    grid:
      x: 6
      y: 36
      w: 6
      h: 4
  - id: database-credential-creations
    title: Database credential creates
    type: stat
    signal: metrics
    datasource: metrics
    expression: openbao:database_new_user:rate5m
    unit: ops
    description: Shows database dynamic credential creation operations per second over 5 minutes.
    grid:
      x: 12
      y: 36
      w: 6
      h: 4
  - id: dynamic-leases-by-engine
    title: Dynamic leases by engine
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: openbao:secret_lease_creation_by_engine:increase15m
    legend: "{{secret_engine}}"
    unit: short
    description: Shows dynamic secret lease creation events by bounded secret engine label over 15 minutes.
    grid:
      x: 0
      y: 40
      w: 8
      h: 8
  - id: pki-issue-revoke-rates
    title: PKI issue and revoke rates
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:pki_issue:rate5m, "pki_operation", "issue", "source_prefix", ".*") or label_replace(openbao:pki_revoke:rate5m, "pki_operation", "revoke", "source_prefix", ".*")
    legend: "{{pki_operation}}"
    unit: ops
    description: Shows PKI certificate issue and revoke operation rates from normalized recording rules.
    grid:
      x: 8
      y: 40
      w: 8
      h: 8
  - id: database-credential-operation-rates
    title: Database credential operation rates
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:database_new_user:rate5m, "database_operation", "create", "source_prefix", ".*") or label_replace(openbao:database_update_user:rate5m, "database_operation", "renew", "source_prefix", ".*") or label_replace(openbao:database_delete_user:rate5m, "database_operation", "revoke", "source_prefix", ".*")
    legend: "{{database_operation}}"
    unit: ops
    description: Shows database dynamic credential create, renew, and revoke operation rates.
    grid:
      x: 16
      y: 40
      w: 8
      h: 8
  - id: pki-issue-revoke-latency
    title: PKI issue and revoke latency
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:pki_issue:avg5m, "pki_operation", "issue", "source_prefix", ".*") or label_replace(openbao:pki_revoke:avg5m, "pki_operation", "revoke", "source_prefix", ".*")
    legend: "{{pki_operation}}"
    unit: ms
    description: Shows average PKI issue and revoke latency over 5 minutes from summary sum and count samples.
    grid:
      x: 0
      y: 48
      w: 12
      h: 8
  - id: database-credential-operation-latency
    title: Database credential operation latency
    type: timeseries
    signal: metrics
    datasource: metrics
    expression: label_replace(openbao:database_new_user:avg5m, "database_operation", "create", "source_prefix", ".*") or label_replace(openbao:database_update_user:avg5m, "database_operation", "renew", "source_prefix", ".*") or label_replace(openbao:database_delete_user:avg5m, "database_operation", "revoke", "source_prefix", ".*")
    legend: "{{database_operation}}"
    unit: ms
    description: Shows average database dynamic credential create, renew, and revoke latency over 5 minutes.
    grid:
      x: 12
      y: 48
      w: 12
      h: 8
  - id: secret-engine-events-by-node
    title: Secret engine events by node
    type: timeseries
    signal: logs
    datasource: logs
    expression: 'sum by (node_id) (count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret|kv-v1|database|transit|pki)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    legend: "{{node_id}}"
    unit: short
    description: Shows filtered secret engine audit event volume by OpenBao node over 5 minutes.
    grid:
      x: 0
      y: 56
      w: 12
      h: 8
  - id: mount-events-by-node
    title: Mount lifecycle by node
    type: timeseries
    signal: logs
    datasource: logs
    expression: 'sum by (node_id) (count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"sys/mounts(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    legend: "{{node_id}}"
    unit: short
    description: Shows mount table and lifecycle audit event volume by OpenBao node over 5 minutes.
    grid:
      x: 12
      y: 56
      w: 12
      h: 8
  - id: kv-database-transit-events-by-node
    title: KV, database, and Transit by node
    type: timeseries
    signal: logs
    datasource: logs
    expression: 'sum by (node_id) (count_over_time({log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret/(data|metadata)/.*|kv-v1/.*|database/(config|roles|creds|rotate-root).*|transit/(keys|encrypt|decrypt|rewrap|sign|verify|hmac).*)" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}" [5m]))'
    legend: "{{node_id}}"
    unit: short
    description: Shows common secret engine activity by OpenBao node over 5 minutes.
    grid:
      x: 0
      y: 64
      w: 24
      h: 8
  - id: mount-lifecycle-log
    title: Mount lifecycle events
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"sys/mounts(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows audited mount table reads and mount lifecycle changes.
    grid:
      x: 0
      y: 72
      w: 12
      h: 9
  - id: kv-events-log
    title: KV v1 and v2 events
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret/(data|metadata)/.*|kv-v1/.*)" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows audited KV v1 and KV v2 data and metadata requests.
    grid:
      x: 12
      y: 72
      w: 12
      h: 9
  - id: database-events-log
    title: Database events
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"database/(config|roles|creds|rotate-root).*" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows audited database secrets engine requests.
    grid:
      x: 0
      y: 81
      w: 12
      h: 9
  - id: transit-events-log
    title: Transit events
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"transit/(keys|encrypt|decrypt|rewrap|sign|verify|hmac).*" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows audited Transit key and cryptographic operation requests.
    grid:
      x: 12
      y: 81
      w: 12
      h: 9
  - id: secret-engine-error-log
    title: Secret engine response errors
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json audit_type="type", audit_error="error", operation="request.operation", request_id="request.id", request_path="request.path" | audit_type="response" | audit_error!="" | request_path=~"(secret|kv-v1|database|transit|pki)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows audited secret engine response entries that include error fields.
    grid:
      x: 0
      y: 90
      w: 12
      h: 9
  - id: filtered-secret-engine-stream
    title: Filtered secret engine stream
    type: logs
    signal: logs
    datasource: logs
    expression: '{log_stream="openbao.audit", node_id=~"${node_id:raw}"} | json operation="request.operation", request_id="request.id", request_path="request.path" | request_path=~"(secret|kv-v1|database|transit|pki|sys/mounts)(/.*)?" | request_path=~"${engine_path:raw}" | request_id=~"${request_id:raw}" | operation=~"${operation:raw}"'
    description: Shows the full filtered stream for secret engine and mount investigation.
    grid:
      x: 12
      y: 90
      w: 12
      h: 9
