# Code generated by openbao-observability; DO NOT EDIT.
apiVersion: openbao.observability/v1alpha1
kind: LokiAlertRules
metadata:
    name: openbao-loki-security-alerts
spec:
    groups:
        - name: openbao.loki.alerts
          rules:
            - alert: OpenBaoAuditConfigurationActivity
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.audit"} | json operation="request.operation", request_path="request.path" | operation=~"create|update|delete" | request_path=~"sys/audit(/.*)?" [5m])) > 0
              for: 0m
              labels:
                category: security-detection
                component: audit
                detection: audit-configuration
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: OpenBao audit logs show create, update, or delete activity against audit configuration paths.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/security-audit-detections.md
                summary: OpenBao audit configuration changed
            - alert: OpenBaoRootPolicyMutation
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.audit"} | json operation="request.operation", request_path="request.path" | operation=~"create|update|delete" | request_path=~"sys/policies/(acl/)?root" [5m])) > 0
              for: 0m
              labels:
                category: security-detection
                component: policy
                detection: root-policy
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: OpenBao audit logs show create, update, or delete activity against the root policy path.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/security-audit-detections.md
                summary: OpenBao root policy changed
            - alert: OpenBaoPrivilegedConfigurationMutation
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.audit"} | json operation="request.operation", request_path="request.path" | operation=~"create|update|delete" | request_path=~"sys/(policies|auth|mounts)(/.*)?" [5m])) > 0
              for: 0m
              labels:
                category: security-detection
                component: security
                detection: privileged-configuration
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: OpenBao audit logs show create, update, or delete activity against policy, auth method, or mount configuration paths.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/security-audit-detections.md
                summary: OpenBao privileged configuration changed
            - alert: OpenBaoHighRiskSystemPathActivity
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.audit"} | json request_path="request.path" | request_path=~"sys/(raw|plugins|storage/raft|rotate)(/.*)?" [5m])) > 0
              for: 0m
              labels:
                category: security-detection
                component: security
                detection: high-risk-system-path
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: OpenBao audit logs show activity against high-risk system paths such as raw storage, plugins, Raft storage, or rotation endpoints.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/security-audit-detections.md
                summary: OpenBao high-risk system path activity observed
            - alert: OpenBaoPermissionDeniedBurst
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.audit"} | json audit_type="type", audit_error="error" | audit_type="response" | audit_error=~"(?s).*permission denied.*" [5m])) > 10
              for: 5m
              labels:
                category: security-detection
                component: audit
                detection: permission-denied-burst
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: OpenBao audit logs show more than ten permission denied responses over 5 minutes.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/security-audit-detections.md
                summary: OpenBao permission denied responses increased
            - alert: OpenBaoCompletedRequestLoggingObserved
              expr: sum by (cluster, kubernetes_namespace) (count_over_time({log_stream="openbao.completed_requests"}[5m])) > 0
              for: 5m
              labels:
                category: security-posture
                component: logging
                detection: completed-request-logging
                severity: warning
                signal: loki
                source_prefix: openbao
              annotations:
                description: Loki has received completed request logs. Allow these logs only during an approved troubleshooting window.
                runbook_url: https://github.com/dc-tec/openbao-observability/blob/main/docs/runbooks/completed-request-logging-enabled.md
                summary: OpenBao completed request logging is active
