# Code generated by openbao-observability; DO NOT EDIT.
groups:
    - name: openbao.recording
      rules:
        - record: openbao:core_active:sum
          expr: sum(vault_core_active)
          labels:
            source_prefix: vault
        - record: openbao:core_unsealed:sum
          expr: sum(vault_core_unsealed{cluster!=""})
          labels:
            source_prefix: vault
        - record: openbao:raft_peers:max
          expr: max(vault_raft_peers) or count(count by (peer_id) (vault_raft_storage_stats_commit_index))
          labels:
            source_prefix: vault
        - record: openbao:autopilot_healthy:max
          expr: max(vault_autopilot_healthy)
          labels:
            source_prefix: vault
        - record: openbao:autopilot_failure_tolerance:max
          expr: max(vault_autopilot_failure_tolerance)
          labels:
            source_prefix: vault
        - record: openbao:autopilot_node_healthy:min
          expr: min by (node_id) (vault_autopilot_node_healthy)
          labels:
            source_prefix: vault
        - record: openbao:audit_log_request_failure:increase5m
          expr: sum(increase(vault_audit_log_request_failure[5m]))
          labels:
            source_prefix: vault
        - record: openbao:audit_log_response_failure:increase5m
          expr: sum(increase(vault_audit_log_response_failure[5m]))
          labels:
            source_prefix: vault
        - record: openbao:audit_log_request:rate5m
          expr: sum(rate(vault_audit_log_request_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:audit_log_response:rate5m
          expr: sum(rate(vault_audit_log_response_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:audit_log_request:avg5m
          expr: sum(rate(vault_audit_log_request_sum[5m])) / clamp_min(sum(rate(vault_audit_log_request_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:audit_log_response:avg5m
          expr: sum(rate(vault_audit_log_response_sum[5m])) / clamp_min(sum(rate(vault_audit_log_response_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:expire_num_leases:max
          expr: max(vault_expire_num_leases)
          labels:
            source_prefix: vault
        - record: openbao:expire_num_irrevocable_leases:max
          expr: max(vault_expire_num_irrevocable_leases)
          labels:
            source_prefix: vault
        - record: openbao:expire_revoke:rate5m
          expr: sum(rate(vault_expire_revoke_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:expire_revoke:avg5m
          expr: sum(rate(vault_expire_revoke_sum[5m])) / clamp_min(sum(rate(vault_expire_revoke_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:expire_register_auth:rate5m
          expr: sum(rate(vault_expire_register_auth_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:expire_register_auth:avg5m
          expr: sum(rate(vault_expire_register_auth_sum[5m])) / clamp_min(sum(rate(vault_expire_register_auth_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:token_count:max30m
          expr: max(max_over_time(vault_token_count[30m]))
          labels:
            source_prefix: vault
        - record: openbao:token_creation:increase15m
          expr: sum(increase(vault_token_creation[15m]))
          labels:
            source_prefix: vault
        - record: openbao:token_creation_by_auth:increase15m
          expr: sum by (auth_method) (increase(vault_token_creation[15m]))
          labels:
            source_prefix: vault
        - record: openbao:token_create:rate5m
          expr: sum(rate(vault_token_create_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:token_create:avg5m
          expr: sum(rate(vault_token_create_sum[5m])) / clamp_min(sum(rate(vault_token_create_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:token_lookup:rate5m
          expr: sum(rate(vault_token_lookup_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:token_lookup:avg5m
          expr: sum(rate(vault_token_lookup_sum[5m])) / clamp_min(sum(rate(vault_token_lookup_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:token_store:rate5m
          expr: sum(rate(vault_token_store_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:token_store:avg5m
          expr: sum(rate(vault_token_store_sum[5m])) / clamp_min(sum(rate(vault_token_store_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:token_revoke_tree:rate5m
          expr: sum(rate(vault_token_revoke_tree_count[5m]))
          labels:
            source_prefix: vault
        - record: openbao:token_revoke_tree:avg5m
          expr: sum(rate(vault_token_revoke_tree_sum[5m])) / clamp_min(sum(rate(vault_token_revoke_tree_count[5m])), 0.001)
          labels:
            source_prefix: vault
        - record: openbao:runtime_num_goroutines:max
          expr: max(vault_runtime_num_goroutines)
          labels:
            source_prefix: vault
