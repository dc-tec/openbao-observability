# Code generated by openbao-observability; DO NOT EDIT.
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
    name: openbao-recording-rules
spec:
    groups:
        - name: openbao.recording
          rules:
            - record: openbao:core_active:sum
              expr: sum(openbao_core_active)
              labels:
                source_prefix: openbao
            - record: openbao:core_unsealed:sum
              expr: sum(openbao_core_unsealed{cluster!=""})
              labels:
                source_prefix: openbao
            - record: openbao:core_handle_request:rate5m
              expr: sum(rate(openbao_core_handle_request_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:core_handle_request:avg5m
              expr: sum(rate(openbao_core_handle_request_sum[5m])) / clamp_min(sum(rate(openbao_core_handle_request_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:core_handle_login_request:rate5m
              expr: sum(rate(openbao_core_handle_login_request_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:core_handle_login_request:avg5m
              expr: sum(rate(openbao_core_handle_login_request_sum[5m])) / clamp_min(sum(rate(openbao_core_handle_login_request_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:core_check_token:rate5m
              expr: sum(rate(openbao_core_check_token_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:core_check_token:avg5m
              expr: sum(rate(openbao_core_check_token_sum[5m])) / clamp_min(sum(rate(openbao_core_check_token_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:core_in_flight_requests:max
              expr: max(openbao_core_in_flight_requests)
              labels:
                source_prefix: openbao
            - record: openbao:core_mount_table_num_entries:max
              expr: max by (local, type) (openbao_core_mount_table_num_entries)
              labels:
                source_prefix: openbao
            - record: openbao:core_mount_table_size:max
              expr: max by (local, type) (openbao_core_mount_table_size)
              labels:
                source_prefix: openbao
            - record: openbao:raft_peers:max
              expr: max(openbao_raft_peers) or count(count by (peer_id) (openbao_raft_storage_stats_commit_index))
              labels:
                source_prefix: openbao
            - record: openbao:autopilot_healthy:max
              expr: max(openbao_autopilot_healthy)
              labels:
                source_prefix: openbao
            - record: openbao:autopilot_failure_tolerance:max
              expr: max(openbao_autopilot_failure_tolerance)
              labels:
                source_prefix: openbao
            - record: openbao:autopilot_node_healthy:min
              expr: min by (node_id) (openbao_autopilot_node_healthy)
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_request_failure:increase5m
              expr: sum(increase(openbao_audit_log_request_failure[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_response_failure:increase5m
              expr: sum(increase(openbao_audit_log_response_failure[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_request:rate5m
              expr: sum(rate(openbao_audit_log_request_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_response:rate5m
              expr: sum(rate(openbao_audit_log_response_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_request:avg5m
              expr: sum(rate(openbao_audit_log_request_sum[5m])) / clamp_min(sum(rate(openbao_audit_log_request_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:audit_log_response:avg5m
              expr: sum(rate(openbao_audit_log_response_sum[5m])) / clamp_min(sum(rate(openbao_audit_log_response_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:expire_num_leases:max
              expr: max(openbao_expire_num_leases)
              labels:
                source_prefix: openbao
            - record: openbao:expire_num_irrevocable_leases:max
              expr: max(openbao_expire_num_irrevocable_leases)
              labels:
                source_prefix: openbao
            - record: openbao:expire_revoke:rate5m
              expr: sum(rate(openbao_expire_revoke_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:expire_revoke:avg5m
              expr: sum(rate(openbao_expire_revoke_sum[5m])) / clamp_min(sum(rate(openbao_expire_revoke_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:expire_register_auth:rate5m
              expr: sum(rate(openbao_expire_register_auth_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:expire_register_auth:avg5m
              expr: sum(rate(openbao_expire_register_auth_sum[5m])) / clamp_min(sum(rate(openbao_expire_register_auth_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:token_count:max30m
              expr: max(max_over_time(openbao_token_count[30m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_creation:increase15m
              expr: sum(increase(openbao_token_creation[15m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_creation_by_auth:increase15m
              expr: sum by (auth_method) (increase(openbao_token_creation[15m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_create:rate5m
              expr: sum(rate(openbao_token_create_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_create:avg5m
              expr: sum(rate(openbao_token_create_sum[5m])) / clamp_min(sum(rate(openbao_token_create_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:token_lookup:rate5m
              expr: sum(rate(openbao_token_lookup_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_lookup:avg5m
              expr: sum(rate(openbao_token_lookup_sum[5m])) / clamp_min(sum(rate(openbao_token_lookup_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:token_store:rate5m
              expr: sum(rate(openbao_token_store_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_store:avg5m
              expr: sum(rate(openbao_token_store_sum[5m])) / clamp_min(sum(rate(openbao_token_store_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:token_revoke_tree:rate5m
              expr: sum(rate(openbao_token_revoke_tree_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:token_revoke_tree:avg5m
              expr: sum(rate(openbao_token_revoke_tree_sum[5m])) / clamp_min(sum(rate(openbao_token_revoke_tree_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_num_goroutines:max
              expr: max(openbao_runtime_num_goroutines)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_alloc_bytes:max
              expr: max(openbao_runtime_alloc_bytes)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_heap_objects:max
              expr: max(openbao_runtime_heap_objects)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_sys_bytes:max
              expr: max(openbao_runtime_sys_bytes)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_gc_pause_ns:avg5m
              expr: sum(rate(openbao_runtime_gc_pause_ns_sum[5m])) / clamp_min(sum(rate(openbao_runtime_gc_pause_ns_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_total_gc_pause_ns:max
              expr: max(openbao_runtime_total_gc_pause_ns)
              labels:
                source_prefix: openbao
            - record: openbao:runtime_total_gc_runs:max
              expr: max(openbao_runtime_total_gc_runs)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_get:rate5m
              expr: sum(rate(openbao_barrier_get_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:barrier_get:avg5m
              expr: sum(rate(openbao_barrier_get_sum[5m])) / clamp_min(sum(rate(openbao_barrier_get_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_put:rate5m
              expr: sum(rate(openbao_barrier_put_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:barrier_put:avg5m
              expr: sum(rate(openbao_barrier_put_sum[5m])) / clamp_min(sum(rate(openbao_barrier_put_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_list:rate5m
              expr: sum(rate(openbao_barrier_list_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:barrier_list:avg5m
              expr: sum(rate(openbao_barrier_list_sum[5m])) / clamp_min(sum(rate(openbao_barrier_list_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_list_page:rate5m
              expr: sum(rate(openbao_barrier_list_page_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:barrier_list_page:avg5m
              expr: sum(rate(openbao_barrier_list_page_sum[5m])) / clamp_min(sum(rate(openbao_barrier_list_page_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_delete:rate5m
              expr: sum(rate(openbao_barrier_delete_count[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:barrier_delete:avg5m
              expr: sum(rate(openbao_barrier_delete_sum[5m])) / clamp_min(sum(rate(openbao_barrier_delete_count[5m])), 0.001)
              labels:
                source_prefix: openbao
            - record: openbao:barrier_estimated_encryptions:increase15m
              expr: sum by (term) (increase(openbao_barrier_estimated_encryptions[15m]))
              labels:
                source_prefix: openbao
            - record: openbao:cache_hit:rate5m
              expr: sum(rate(openbao_cache_hit[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:cache_miss:rate5m
              expr: sum(rate(openbao_cache_miss[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:cache_write:rate5m
              expr: sum(rate(openbao_cache_write[5m]))
              labels:
                source_prefix: openbao
            - record: openbao:cache_hit_ratio:ratio5m
              expr: sum(rate(openbao_cache_hit[5m])) / clamp_min(sum(rate(openbao_cache_hit[5m])) + sum(rate(openbao_cache_miss[5m])), 0.001)
              labels:
                source_prefix: openbao
